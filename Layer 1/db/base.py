# FIle is used to create a declarative base instant that is used in other files
# Declaritive base is used for defining mapped classes

from sqlalchemy.orm import declarative_base


Base = declarative_base()
